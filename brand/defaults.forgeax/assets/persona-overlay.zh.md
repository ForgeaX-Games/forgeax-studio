# ForgeaX persona overlay (zh)

> Stub。White-label brand pack 可以在这里覆盖默认 persona 段头 `## OVERRIDE:assistant-name`
> 等区段。当前主仓 marketplace 把 assistant 名字（`Forge`）直接写死在
> `packages/marketplace/src/system-prompt/00-persona.zh.md`，本文件仅为白标预留。

## OVERRIDE:assistant-name

Forge
