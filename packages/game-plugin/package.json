{
  "packageManager": "bun@1.4.0",
  "name": "@forgeax/game",
  "version": "0.2.4",
  "private": false,
  "type": "module",
  "description": "@forgeax/game \u2014 ForgeaX game development as a plugin for MCP-capable agent CLIs (Codex, the reference agent CLI, Cursor, Trae, ZCode, OpenCode, WorkBuddy). Single binary, dual mode: no args = stdio MCP server, subcommand = CLI.",
  "main": "./dist/main.js",
  "bin": {
    "forgeax-game": "dist/main.js"
  },
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "files": [
    "dist",
    "assets/skills",
    "README.md"
  ],
  "scripts": {
    "build": "bun build.mjs",
    "release:check": "bun scripts/check-package-artifact.ts",
    "acceptance": "bun scripts/accept-packed-consumer.ts",
    "release:publish": "bun scripts/publish-package.ts",
    "prepack": "bun build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "bun test test/*.test.ts",
    "start": "bun src/main.ts",
    "mcp:inspect": "npx @modelcontextprotocol/inspector node dist/main.js"
  },
  "keywords": [
    "forgeax",
    "mcp",
    "model-context-protocol",
    "game-development",
    "codex",
    "claude-code"
  ],
  "license": "MIT",
  "dependencies": {
    "@forgeax/game-runtime": "0.3.33"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.9.2"
  }
}
