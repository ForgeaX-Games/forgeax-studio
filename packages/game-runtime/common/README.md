# @forgeax/game-runtime-common

Shared Runtime verification, cache, launch, environment, port, manifest, and Engine SDK APIs used by ForgeaX platform Runtime packages.

Consumers normally install `@forgeax/game-runtime`, not this package directly. Platform packages create a distribution with explicit package roots; this package never searches for a neighboring Game or Studio checkout.
