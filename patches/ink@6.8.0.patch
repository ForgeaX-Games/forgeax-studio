diff --git a/build/ink.js b/build/ink.js
index 30019a073d38ed05dca08f6221af187c429a536b..176f08be15275a1fe8ef1749dbd35a8187f74120 100644
--- a/build/ink.js
+++ b/build/ink.js
@@ -319,21 +319,12 @@ export default class Ink {
         // Only apply when writing to a real TTY — piped output always gets trailing newlines.
         const isFullscreen = this.options.stdout.isTTY && outputHeight >= this.options.stdout.rows;
         const outputToRender = isFullscreen ? output : output + '\n';
-        if (this.lastOutputHeight >= this.options.stdout.rows) {
-            const sync = shouldSynchronize(this.options.stdout);
-            if (sync) {
-                this.options.stdout.write(bsu);
-            }
-            this.options.stdout.write(ansiEscapes.clearTerminal + this.fullStaticOutput + output);
-            this.lastOutput = output;
-            this.lastOutputToRender = outputToRender;
-            this.lastOutputHeight = outputHeight;
-            this.log.sync(outputToRender);
-            if (sync) {
-                this.options.stdout.write(esu);
-            }
-            return;
-        }
+        // [forgeax patch · 对照参考实现 patches/ink+6.3.1.patch]
+        //   原版:动态内容超过终端高度(lastOutputHeight >= rows)时走
+        //     `clearTerminal + fullStaticOutput + output` —— 整屏清(含 scrollback)+ 重灌全部
+        //     Static。真终端上(尤其 resize/SIGWINCH 把 pane 挤窄触发本分支)会把历史滚动 + 动态帧
+        //     叠加 = 输入框重复/残影。改为「落到下面的增量路径」(log.clear + 写新 Static + 增量 log
+        //     动态区),不清屏不重灌,与参考实现同策。isFullscreen/outputToRender 仍供下方使用。
         // To ensure static output is cleanly rendered before main output, clear main output first
         if (hasStaticOutput) {
             const sync = shouldSynchronize(this.options.stdout);
@@ -550,6 +541,19 @@ export default class Ink {
             this.log.sync(this.lastOutputToRender || this.lastOutput + '\n');
         }
     }
+    // [forgeax patch · 对照参考实现 patches/ink+6.3.1.patch]
+    //   重置 Static 累加器 + 动态区记账,供 app 在 resize/clear 时做干净全量重绘:
+    //   配合「写 clearTerminal + 给 <Static> 换 key 重挂载重新 emit」使用,绕开 resized()
+    //   的 eraseLines 在终端 reflow 后擦错行数导致的残影。
+    resetStaticOutput() {
+        this.fullStaticOutput = '';
+        this.lastOutput = '';
+        this.lastOutputToRender = '';
+        this.lastOutputHeight = 0;
+        if (!isInCi && !this.options.debug) {
+            this.log.clear();
+        }
+    }
     patchConsole() {
         if (this.options.debug) {
             return;
diff --git a/build/reconciler.js b/build/reconciler.js
index 03c7d45d61c0c8abc9313126a264951c8ece0b8d..62a519310cbd4ac31312337ada85cb01416e1f07 100644
--- a/build/reconciler.js
+++ b/build/reconciler.js
@@ -160,9 +160,28 @@ export default createReconciler({
     unhideInstance(node) {
         node.yogaNode?.setDisplay(Yoga.DISPLAY_FLEX);
     },
-    appendInitialChild: appendChildNode,
-    appendChild: appendChildNode,
-    insertBefore: insertBeforeNode,
+    // [forgeax patch · 对照参考实现 patches/ink+6.3.1.patch]
+    //   往 <Static> 追加/插入子节点时标记 isStaticDirty,触发**立即(非节流)**重渲。
+    //   否则节流 onRender 若在 useLayoutEffect 更新 index 之后才跑,新 Static 条目会丢
+    //   (itemsToRender 为空)。6.8.0 原版只在 removeChild 标 dirty,append/insert 漏了。
+    appendInitialChild(node, childNode) {
+        appendChildNode(node, childNode);
+        if (node.internal_static && currentRootNode) {
+            currentRootNode.isStaticDirty = true;
+        }
+    },
+    appendChild(node, childNode) {
+        appendChildNode(node, childNode);
+        if (node.internal_static && currentRootNode) {
+            currentRootNode.isStaticDirty = true;
+        }
+    },
+    insertBefore(node, newChildNode, beforeChildNode) {
+        insertBeforeNode(node, newChildNode, beforeChildNode);
+        if (node.internal_static && currentRootNode) {
+            currentRootNode.isStaticDirty = true;
+        }
+    },
     finalizeInitialChildren() {
         return false;
     },
diff --git a/build/render.d.ts b/build/render.d.ts
index ba32a3e8d0eeab6b304a6d36c55cda0e1c7a2f2d..18e8dd0209e899f34f93a9c5c31aecab60daaaa2 100644
--- a/build/render.d.ts
+++ b/build/render.d.ts
@@ -113,6 +113,12 @@ export type Instance = {
     Clear output.
     */
     clear: () => void;
+    /**
+    Reset the Static output accumulator + dynamic bookkeeping (forgeax patch).
+    Call alongside writing clearTerminal and remounting <Static> (via key change)
+    for a clean full redraw on resize/clear without reflow ghosting.
+    */
+    resetStaticOutput: () => void;
 };
 /**
 Mount a component and render the output.
diff --git a/build/render.js b/build/render.js
index 6526022c509778bf8ebf95af33be29b2626934be..3c038b3b565ad7217122a8e524875d98466b8c8f 100644
--- a/build/render.js
+++ b/build/render.js
@@ -28,6 +28,8 @@ const render = (node, options) => {
         waitUntilExit: instance.waitUntilExit,
         cleanup: () => instances.delete(inkOptions.stdout),
         clear: instance.clear,
+        // [forgeax patch · 对照参考实现] 暴露给 app 做 resize/clear 干净重绘。
+        resetStaticOutput: () => instance.resetStaticOutput(),
     };
 };
 export default render;
